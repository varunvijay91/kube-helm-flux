apiVersion: v1
kind: Service
metadata:
  name: {{ include "get_started.fullname" . }}
  labels:
    {{- include "get_started.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "get_started.selectorLabels" . | nindent 4 }}
